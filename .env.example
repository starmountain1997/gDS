# 复制为同目录下的 .env 后填写（.env 已在 .gitignore，勿提交）
# 勿将真实密钥写入本示例文件。
# 已有 export 的变量优先于本文件；可先 unset OPENAI_API_KEY OPENAI_BASE_URL OPENAI_MODEL 再试。

# ── 必填 ──────────────────────────────────────────────────────────────────────
# 你的 API Key（不要加引号，不要有多余空格）
OPENAI_API_KEY=

# ── 可选：API 服务端点（须含 /v1，脚本会自动补全）────────────────────────────
# Kimi（默认）
OPENAI_BASE_URL=https://api.minimaxi.com/v1
# DeepSeek
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# 本地 vLLM / Ollama
# OPENAI_BASE_URL=http://localhost:8000/v1

# ── 可选：模型名（默认 kimi-k2.5）────────────────────────────────────────────
OPENAI_MODEL=MiniMax-M2.7-highspeed
# OPENAI_MODEL=deepseek-chat
# OPENAI_MODEL=your-local-model
