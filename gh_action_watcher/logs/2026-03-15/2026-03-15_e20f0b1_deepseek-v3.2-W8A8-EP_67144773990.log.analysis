类型: 设备环境问题  

**根本原因（≤5 条）**  
1. **节点 10.0.0.101 上的服务器进程未能成功启动或持续崩溃**——Pod 多次重启（3 次）表明容器内部可能出现致命错误（OOM、GPU 不可用、库缺失等），导致 /health 端点始终不可达。  
2. **资源调度或配额不足**——该 Pod 可能未被调度到具备所需 GPU、内存或特定硬件的节点，导致进程启动失败。  
3. **网络通信受阻**——测试机向 `http://10.0.0.101:8080/health` 发起的 HTTP 请求失败，可能是防火墙、NetworkPolicy 或 Pod 网络配置错误。  
4. **健康检查端点实现/绑定错误**——服务器可能只在 master IP（即 `config.master_ip`）上监听，而未在所有节点的可路由地址上绑定，导致 10.0.0.101 不可访问。  
5. **容器启动脚本或入口点异常**——启动命令、依赖库或环境变量未正确传递，使得实际进程在后台异常退出，未产生可观测的日志。  

**最可能的修复方案**  
1. **检查目标 Pod 日志**：`kubectl logs -n vllm-project vllm-deepseek-v3-2-w8a8-ep-0-1`（以及 `-0-2`、`-0-3`），确认是否有 OOM、CUDA 错误或库缺失等异常。  
2. **验证资源分配**：确保该 Pod 的资源请求（GPU、内存）与节点的实际可用资源匹配；检查节点标签与污点是否导致调度到错误节点。  
3. **审查网络策略**：确认 ClusterNetwork、NetworkPolicy 是否允许 8080 端口的跨 Pod 流量；或在 Pod 中使用 `curl` 手动验证 `/health` 是否返回 200。  
4. **确认服务绑定**：在服务器启动参数中显式指定 `--host 0.0.0.0` 或 `--server-host` 为 Pod 的 IP，确保健康检查端点在所有网络接口上可访问。  
5. **增强健康检查容错**：在测试脚本或服务器实现中加入启动阶段的“预热”日志（如 “Server started, listening on …”），并在超时前使用更细粒度的重试（如每 10 秒一次），帮助快速定位何时何因导致不可用。  

通过上述步骤可以定位是资源不足、网络不通还是服务本身启动异常，从而针对性地修复环境或代码，使两节点均能在规定时间内就绪。