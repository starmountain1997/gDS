类型: 设备环境问题  

根本原因（≤5 条）  
1. **Worker 进程在初始化时未捕获到底层硬件异常**（如 NPU 驱动未加载、设备不可用或缺少对应算子），导致进程直接退出。  
2. **多节点网络不通或端口冲突**，RemoteOpenAIServer 在等待健康检查时检测到节点 10.0.0.76 的服务异常退出。  
3. **资源不足**（共享内存、GPU/NPU 显存）导致 OOM，尤其是 W8A8 量化模型对内存要求较高。  
4. **模型/量化方式与当前硬件平台不兼容**（如 A3 架构的算子未编译或不支持 W8A8），导致 worker 初始化失败。  
5. **多进程执行框架异常退出后未正确清理共享内存**，resource_tracker 报警 “leaked shared_memory objects”，后续进程因资源泄漏而崩溃。  

最可能的修复方案  
- **检查并确保 Ascend NPU 驱动/运行时已正确加载**：运行 `npu‑smi` 或相应诊断脚本确认设备可见，必要时在 CI 环境中重新装载驱动或使用 `ASCEND_VISIBLE_DEVICES` 指定设备。  
- **清理残留共享内存并提高资源限制**：在启动测试前执行 `ipcrm -a` 或 `rm -rf /dev/shm/*`，并设置 `ulimit -l unlimited`、`ulimit -shm` 足够大（如 64 GB），防止 OOM。  
- **验证多节点网络连通性**：确保所有节点能够互相 SSH、端口 8080（健康检查）未被占用且防火墙已放行；必要时在测试脚本中加入 `ping` 或 `nc` 检查。  
- **确认模型与量化方案的兼容性**：在目标节点上手动加载一次模型，检查是否出现算子缺失错误；如不支持 A3/W8A8，可先在 CPU 或兼容的 GPU 环境下通过 `--device cpu` 验证，或在 CI 中切换到已验证的模型分支。  
- **捕获并传播 WorkerProc 的真实异常**：在 `multiproc_executor.py` 的 `wait_for_ready` 中将底层异常信息打印或写入日志，或在 CI 环境变量中设置 `VLLM_WORKER_DEBUG=1`，以便后续定位根本错误。  

通过上述步骤可以定位是硬件/驱动问题、资源不足还是模型兼容性问题，并针对性地修复，使 Engine Core 能够在多节点环境下成功启动并通过健康检查。