类型: 性能精度不达标

根本原因（≤5条）：
1. 模型推理输出为空或无效内容，导致后处理器提取失败，最终准确率为0。
2. 后处理器 `extract_non_reasoning_content` 与模型输出格式不匹配，无法正确提取非推理内容。
3. GSM8K 数据集加载或预处理异常，可能未提供有效输入给模型。
4. 多节点推理配置下，节点间通信或同步失败，导致部分请求未完成。
5. 模型量化配置（W8A8）可能导致精度下降，影响推理结果正确性。

最可能的修复方案：
1. 在测试环境中添加模型输出调试日志，确认推理是否成功并打印原始输出，以便定位输出问题。
2. 检查 `pred_postprocessor` 配置，确保 `extract_non_reasoning_content` 逻辑与模型输出格式一致，或暂时禁用后处理器进行对比测试。
3. 验证数据集完整性，确认 `/root/.cache/modelscope/hub/datasets/vllm-ascend/gsm8k-lite` 已正确下载且可用。
4. 检查多节点网络配置，确保所有节点在 `10.0.0.203:8080` 上正常通信，并验证代理或 disaggregated prefill 设置是否正确。
5. 尝试使用非量化模型（如 FP16）进行相同测试，以判断量化是否导致精度问题，并调整量化参数或启用混合精度。
