类型: 设备环境问题

根本原因：
1. **vLLM服务器请求处理失败**：AISBench报告所有请求失败，无法计算性能结果。日志中AISBench生成了HTML报告文件（gsm8kdataset_plot.html），说明测试有执行但在获取响应时全部失败。
2. **模型服务可能未完全就绪**：测试使用的模型 `vllm-ascend/DeepSeek-V3.2-W8A8` 是量化版本（W8A8），在Ascend NPU上可能存在兼容性或初始化问题，导致请求无法被正确处理。
3. **服务器启动验证缺失**：测试直接向 `10.0.0.141:6000` 发送请求，但没有等待服务器完全加载模型并进入可服务状态的确认机制。
4. **可能的资源不足**：量化模型在NPU上可能需要更多显存或内存资源，导致请求处理时OOM或超时。
5. **请求超时或网络问题**：测试运行了约14分钟（881.90s），最后在性能计算阶段失败，可能是服务器响应缓慢或频繁超时。

最可能的修复方案：
1. **添加服务器健康检查**：在AISBench运行前，添加对服务器 `/v1/models` 或 `/health` 端点的轮询检查，确保模型完全加载且可服务。
2. **增加请求重试和超时配置**：在aisbench配置中增加 `max_retries` 和 `timeout` 参数，避免因瞬时网络问题导致全部请求失败。
3. **检查NPU驱动和运行时**：确认Ascend NPU的驱动、固件和vLLM Ascend后端版本兼容，特别是对于量化模型的支持。
4. **验证模型权重路径**：确保模型权重正确下载且路径配置无误，量化模型可能对权重格式有特殊要求。
5. **降低测试负载**：暂时减少 `batch_size`（如从512降至256）或减少并发请求数，排查是否为资源争用导致的问题。
