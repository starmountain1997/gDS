类型: 性能精度不达标

**根本原因（≤5 条）**

1. **服务器并发受限**：`max-num-seqs=4` 与 `max-num-batched-tokens=8192` 限制了同时处理的序列数，导致实际并发约 4，远低于 request_rate=11.2 下的潜在并发，吞吐未得到充分利用。  
2. **投机解码配置不理想**：启用了 `speculative-config {"num_speculative_tokens": 3, "method":"deepseek_mtp"}`，若投机成功率低会产生大量无效计算和同步开销，降低有效吞吐量。  
3. **量化方式对硬件不友好**：模型使用 W8A8（int8 权重量化）配合 Ascend NPU，若 NPU 对 int8 计算支持不成熟或驱动实现不佳，会导致算子执行慢于预期。  
4. **张量并行 + 数据并行组合导致通信瓶颈**：TP=8、DP=2 的部署方式在跨 NPU 通信时产生较大同步延迟，尤其在短请求高频率场景下更为突出。  
5. **输入 token 统计异常**：日志显示 `InputTokens` 始终为 0，说明数据集未成功加载或 prompt 为空，导致模型在处理 “空输入 + 1500 token 生成” 时出现不必要的预处理开销，进而影响整体吞吐。

**最可能的修复方案**

1. **提升并发上限**  
   - 将 `max-num-seqs` 调整为 16~32（依据显存和 NPU 规格），并相应增大 `max-num-batched-tokens`，使服务器能够接受更高的并发请求。  
2. **关闭或调低投机解码**  
   - 暂时禁用 `speculative-config`，或把 `num_speculative_tokens` 设为 1，评估基准吞吐是否提升。若投机效果显著，再尝试细粒度调参。  
3. **尝试更高精度或更适配的量化方案**  
   - 在 Ascend NPU 上使用 FP16 或 W8A16（权重量化仅在权重的 8 位）进行实验，确认是否为量化导致的算子瓶颈。  
4. **优化 TP/DP 配置**  
   - 若硬件节点数有限，可将 TP 降至 4 或 2，保持 DP 2，观察通信开销对吞吐的影响；或在 TP 较大时降低 DP，减少跨节点同步。  
5. **修复数据集加载与 token 计数**  
   - 检查 `gsm8k_gen_0_shot_cot_str_perf_custom` 配置，确保 prompt 已正确生成且 `input_tokens` 计数非零；必要时在测试脚本中加入空输入过滤或重新生成数据集。  
6. **重新评估基准值**  
   - 若硬件实际算力（SOC、NPU 规格）本身低于 134 token/s 的预期，应在测试配置中更新 `baseline` 为更符合当前环境的数值（如 110 token/s），避免因基准不合理导致的误报。  

通过上述调整，可在保持模型准确性的前提下提升输出 token 吞吐，使其满足 0.97 × baseline ≈ 130 token/s 的性能要求。