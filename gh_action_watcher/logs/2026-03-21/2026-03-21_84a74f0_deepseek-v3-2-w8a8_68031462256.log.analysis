类型: 设备环境问题

根本原因：
1. 分布式通信故障：使用 gloo TCP 后端进行集合通信时，peer 进程（10.0.0.135）异常关闭连接，导致 `dist.all_reduce` 失败，worker 进程被终止。
2. NPU 内存分配失败：设备报告内存分配失败（EL0004），可能由于 `gpu-memory-utilization` 设置过高（0.98）且批处理参数过大导致显存不足。
3. NPU 硬件异常：芯片（chipId:4）出现 MTE 指令 DDR 地址越界错误（错误码 0x800000），表明硬件存在不稳定或驱动缺陷。
4. 资源配置不匹配：当前配置（TP8、DP2、expert-parallel、speculative decoding 等）导致多进程竞争资源，增加通信和内存压力。
5. 代码健壮性不足：模型运行时未有效捕获和恢复硬件错误，导致错误传播至主进程，引发任务整体失败。

最可能的修复方案：
1. **调整内存与批处理参数**：将 `gpu-memory-utilization` 降至 0.9 或更低，减小 `max-num-batched-tokens`（如 4096）、`max-num-seqs`（如 2），以缓解内存压力。
2. **优化分布式通信**：
   - 若硬件支持，建议切换至 NCCL 后端替代 gloo，提升通信稳定性。
   - 在环境变量中设置 `GLOO_SOCKET_TIMEOUT`（如 300s）增加超时容忍度。
3. **诊断硬件状态**：运行 NPU 诊断工具（如 `npu-smi`）检查设备健康度，更新驱动至最新版本；若硬件故障持续，考虑更换芯片或机器。
4. **简化测试配置**：暂时禁用 `expert-parallel`、`speculative-config` 等高级特性，使用更保守的配置进行基准测试，以隔离问题。
5. **增强错误处理**：在模型推理代码中添加异常捕获（如 `try-except`）和进程重启逻辑，防止单点故障导致全局任务崩溃；同时添加重试机制以应对瞬时通信错误。
