类型: 性能精度不达标

根本原因（≤5 条）：

1. **输入数据未加载**：日志显示 InputTokens 全为 0.0，说明测试数据的输入 token 未被正确解析或传递，导致模型推理缺少必要的输入上下文。
2. **并发度过低**：实际并发度仅 3.94，而请求速率为 11.2 req/s，批处理未能有效聚合请求，导致 GPU 利用率不足。
3. **端到端延迟异常高**：E2EL 平均 88 秒、最长 145 秒，远超正常范围，表明推理过程中存在严重阻塞或资源争抢。
4. **硬件资源瓶颈**：TP8 + DP2 配置对通信带宽和内存要求极高，W8A8 量化可能引入额外计算开销，配合低并发导致 GPU 无法高效执行。
5. **性能基准设定不合理**：基线 134 token/s 可能在当前配置（DeepSeek-V3.2-W8A8、推测解码、Expert Parallel）下难以达到，需根据实际硬件调整。

最可能的修复方案：

1. **修复数据管道**：检查 `gsm8k_gen_0_shot_cot_str_perf_custom.py` 数据集配置，确保 prompt 正确编码并传递 input_ids，避免 InputTokens 为 0 的情况。
2. **提高并发和批处理**：将 `batch_size` 从 4 提升至 8-16，或在服务端启用连续批处理（continuous batching）以更好利用 GPU。
3. **调整或关闭推测解码**：speculative decoding 配置可能不适合当前量化模型，可尝试禁用或减少 `num_speculative_tokens` 至 1。
4. **优化硬件配置**：检查 NCCL/HCCL 通信是否正常，确认 GPU 显存充足（当前 gpu-memory-utilization 已设 0.98，可能过高），考虑降低至 0.9。
5. **重新设定性能基准**：根据当前硬件和模型配置重新测量实际吞吐量作为新基线，或在更高配机器上运行基准测试。
