类型: 设备环境问题

**根本原因（≤5 条）**
1. 容器入口脚本 `/root/.cache/tests/run.sh` 启动后立即退出（ExitCode 1），导致 CrashLoopBackOff；可能是模型配置文件 `DeepSeek‑V3_2‑W8A8‑A3‑dual‑nodes.yaml` 或模型权重缺失、路径错误。
2. Ascend NPU（`huawei.com/ascend‑1980`）设备在容器内不可见或驱动未正确加载，使 vLLM 初始化失败；常见原因为容器未设置 `ASCEND_VISIBLE_DEVICES`、缺少特权模式或挂载的 driver‑tools 与容器内部库不匹配。
3. 使用的 `nightly‑a3` 镜像与主机上的 Ascend 驱动版本不兼容，运行时出现符号找不到或库冲突，导致进程崩溃。
4. Leader‑Worker‑Set 双节点通信（`LWS_LEADER_ADDRESS`）在启动阶段失败，leader 与 follower 均未能进入 ready 状态，可能涉及 DNS 解析或网络策略问题。
5. Pod 资源请求 `huawei.com/ascend‑1980: 16` 与节点实际可用 NPU 数量不匹配，或节点已被其他任务占用，导致调度或运行时错误。

**最可能的修复方案**
1. **确认模型与配置文件存在**
   - 检查 `/vllm‑workspace`（或共享 PVC `nv‑action‑vllm‑benchmarks‑v2`）中是否已有 `DeepSeek‑V3_2‑W8A8‑A3‑dual‑nodes.yaml` 及对应权重。
   - 如缺失，将其复制到共享卷或在镜像构建阶段加入，并在 `run.sh` 中使用绝对路径。

2. **正确暴露 Ascend 设备**
   - 在 Pod spec 中加入环境变量 `ASCEND_VISIBLE_DEVICES=0-15`（或对应的设备范围），并确保容器以 `privileged: true` 运行或具备 `securityContext.capabilities.add: SYS_RAWIO`。
   - 验证宿主机的 `/usr/local/Ascend/driver/tools` 与容器内路径一致，或在 `run.sh` 中 `source` 驱动脚本（如 `set_env.sh`）后再启动 vLLM。

3. **使用经过验证的镜像版本**
   - 替换 `swr.cn‑southwest‑2.myhuaweicloud.com/base_image/ascend‑ci/vllm‑ascend:nightly‑a3` 为已通过兼容性测试的正式版（如 `vllm‑ascend:stable‑a3`）。
   - 如必须使用 nightly，请在本地重新构建镜像并在 CI 环境中先行单元测试，确保驱动库匹配。

4. **保证 Leader‑Worker 通信畅通**
   - 检查集群 DNS 与网络策略，确保 `vllm‑0.vllm.vllm‑project` 可被 `vllm‑0‑1` 正确解析。
   - 在 `run.sh` 中加入对 leader 就绪的轮询或等待时间（例如 `sleep 30 && curl -f http://${LWS_LEADER_ADDRESS}:8080/ready`），防止 follower 因网络超时而直接退出。

5. **调整资源请求或节点选择**
   - 若节点只有 8 张 Ascend‑1980，则将 Pod 资源请求改为 `huawei.com/ascend‑1980: 8`，或使用节点标签 `ascend‑device‑count=16` 进行精准调度。
   - 在 `lws.yaml` 中为每个 Pod 添加 `nodeSelector` 或 `tolerations`，确保调度到满足资源需求的节点。

**补充建议**
- 在 `run.sh` 中捕获并写入日志：`exec &> /tmp/vllm_leader.log`，并通过 volume 将 `/tmp/vllm*_logs.txt` 挂载到主机，便于后续调试。
- 若仍出现退出码 1，建议在 `run.sh` 开头添加 `set -x`，并检查 `kubectl logs <pod> --previous` 获取最近一次崩溃的具体错误信息。
