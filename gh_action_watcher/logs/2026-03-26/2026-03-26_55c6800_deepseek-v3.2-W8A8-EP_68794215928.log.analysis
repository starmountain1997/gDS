类型: 设备环境问题

**根本原因（≤5 条）**
1. **Pod 重复重启**：所有 vllm‑deepseek‑v3‑2‑w8a8‑ep‑* Pod 均出现 1 次重启，表明 vLLM 服务进程在评估期间因资源不足（最可能是 OOM）崩溃。
2. **批大小超内存**：性能评估使用 `batch_size=512`，在 DeepSeek‑V3.2‑W8A8 模型及多节点 EP 配置下，GPU 显存或主机内存不足导致请求失败。
3. **缺乏容错机制**：AISBench 在任意请求返回错误（如 HTTP 500、超时）时直接标记测试失败，未配置重试或容忍少量失败的策略。
4. **Disaggregated Prefill 实验特性**：该模式在多节点间进行 prefill 分割，同步或通信故障可能产生部分请求错误。
5. **资源限制或调度超时**：容器资源限制（CPU/内存/GPU）与 `max_wait_seconds=2800` 不匹配，导致在高负载时出现超时，进一步触发请求失败。

**最可能的修复方案**
1. **降低批处理规模**：将 `batch_size` 降至 256 或更小，确保显存和内存不溢出；或采用分阶段批处理以减轻单次压力。
2. **提升容器资源**：增大容器内存/显存限制，或为 Pod 配置 `memory request/limit`、`nvidia.com/gpu` 合适的配额，必要时开启 swap。
3. **加入容错与重试**：在 AISBench 配置中打开 “允许少量错误” 选项，或在脚本层面实现对偶发错误的自动重试。
4. **审查并稳定 Disaggregated Prefill**：若实验特性非必需，可暂时关闭；若必须使用，需检查节点间网络、调度同步及配置参数（如 `disagg_cfg`）的一致性。
5. **完善健康检查与日志**：在 vLLM 服务中加入就绪/存活探针，确保在资源紧张时被 Kubernetes 正确重启而非产生静默错误；并在失败后保留完整的错误日志供后续定位。
