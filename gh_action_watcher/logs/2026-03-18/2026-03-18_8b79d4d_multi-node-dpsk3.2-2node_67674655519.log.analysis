类型: 性能精度不达标

根本原因（≤5 条）
1. **模型推理返回空或无效输出**：在 CPU 上运行 vLLM API 时可能因后端未正确加载或加速库缺失，导致模型只返回空响应，评估脚本提取不到答案，准确率为 0。
2. **答案解析不匹配**：测评脚本使用的答案提取正则（如 “答案：(\-?\d+)”）与模型实际输出格式不一致，导致解析失败，误判为 0。
3. **数据集或提示词不一致**：测评使用的 `gsm8k-lite` 与基线训练的 `gsm8k` 数据分布或提示模板不同，使得模型在该子集上表现极差。
4. **远程/多节点配置错误**：RemoteOpenAIServer、Proxy 或 disaggregated‑prefill 配置错误，使得请求未正确路由或返回 500 错误，aisbench 捕获不到有效结果。
5. **环境变量或依赖缺失**：缺少必要的硬件驱动、CUDA/CANN 库或 vLLM 所需的共享对象，导致推理服务异常退出。

最可能的修复方案
1. **确认模型运行在正确设备**：若测试节点有 GPU，确保 `VLLM_DEVICE=cuda` 或相应加速后端已加载；若无 GPU，验证 vLLM‑CPU 模式是否完整安装并使用正确的镜像。
2. **对齐答案解析逻辑**：在 aisbench 中打印模型原始回复，对比基线日志中的答案格式，必要时调整正则或后处理脚本。
3. **使用与基线相同的数据集和提示**：检查 `dataset_conf` 是否与基线配置完全一致，或在基线环境中重新生成 `gsm8k‑lite` 采样的提示文件。
4. **调试远程/多节点通信**：在 `RemoteOpenAIServer` 启动后手动 curl 一次端点，确认返回 200 并带有有效 JSON；若使用代理，验证 `proxy_port` 与 `server_port` 是否匹配。
5. **补充环境依赖检查**：在 CI 脚本中加入启动前的依赖检查（如 `nvidia-smi`、`cann` 版本、vLLM 共享库是否完整），确保所有必需的库已装载。
