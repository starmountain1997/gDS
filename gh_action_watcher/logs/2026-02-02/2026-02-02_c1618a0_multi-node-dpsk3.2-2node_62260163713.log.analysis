类型: 性能精度不达标  

**根本原因（≤5 条）**  
1. **输入数据异常**：日志中 `InputTokens` 全为 0，说明评测脚本未能正确把 prompt 内容发送给模型，导致推理路径异常或度量不真实。  
2. **端口冲突**：多个 ApiServer（0‑3）均尝试绑定 8080 端口，互相检测到 “port 8080 is used by…”，请求路由或转发很可能出错，造成请求堆积、延迟剧增。  
3. **并发/速率配置失衡**：评测配置的 `request_rate=11.2`、`batch_size=512` 使得实际并发请求数达到 512，系统资源被瞬间占满，吞吐率远低于预期。  
4. **多节点资源分配不足**：在 dual‑nodes 环境下，GPU/CPU 或网络带宽可能未充分分配或调度不均，导致每个 worker 的处理能力受限。  
5. **基线不匹配**：基线 `594.915 token/s` 可能是基于不同硬件或优化后的配置测得的，当前环境难以达到该数值。  

**最可能的修复方案**  
1. **修复数据加载**：检查 `dataset_path` 与 `prompt` 生成逻辑，确保 `InputTokens` > 0；若使用 chat‑template，确认模板正确填充。  
2. **解决端口冲突**：为每个 ApiServer（尤其是 disaggregated prefill/decode 角色）分配独立端口（如 8081、8082…），或在 `ProxyLauncher` 中正确配置端口映射与负载均衡。  
3. **调优并发参数**：先将 `request_rate` 降低至系统可承受的水平（如 2‑3 req/s），逐步增加；同步降低 `batch_size`，观察吞吐变化，以找到最佳并发点。  
4. **验证多节点资源**：确认 GPU 可用、显存足够，网络带宽满足节点间通信；必要时在 k8s/Runner 配置中增加资源配额或使用更高配的节点。  
5. **更新或校准基线**：在当前硬件/软件环境下重新跑一次基准测试，取得实际可达的 `OutputTokenThroughput`，并据此调整阈值（可适当放宽至 0.90‑0.95）或记录为新的基线。