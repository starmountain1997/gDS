类型: 性能精度不达标  

**根本原因**（≤5 条）  
1. **量化模型精度严重下降** – 采用 W8A8（8‑bit 权重/激活）量化的 DeepSeek‑V3.2 在 GSM8K 推理任务上表现极差，导致整体准确率仅 3.12%。  
2. **生成参数不匹配推理任务** – temperature=0.6、top_p=0.95 的随机采样在链式思维推理时引入过多不确定性，容易偏离正确答案。  
3. **后处理逻辑错误** – `pred_postprocessor` 设定为 `extract_non_reasoning_content`，会把推理过程过滤掉，仅留下答案；若模型在推理阶段已出错，答案就会被错误截断。  
4. **模型加载或服务配置错误** – `path=""`（空路径）可能导致系统使用默认/占位模型；`trust_remote_code=False` 可能阻止自定义代码执行，使模型行为不符合预期。  
5. **测试数据集版本或格式不匹配** – 实际使用的是 `gsm8k‑lite`，而非完整版 GSM8K，导致相对基线 95% 的评估结果不具备可比性。  

**最可能的修复方案**  
1. **提升模型精度** – 将量化方式改为 W8A16、FP16 或 BF16，或在量化后执行 Calibration/QAT，以恢复数学推理能力。  
2. **优化生成超参数** – 对推理类任务使用更低温度（temperature≈0.1）或直接 greedy（temperature=0），并适当降低 top_p（如 0.9）或启用 top_k，以获得更稳定的答案。  
3. **修正后处理逻辑** – 检查 `extract_non_reasoning_content` 是否正确保留完整思考链（若模型输出包含 “Let’s think step by step”），确保在提取答案前已完成正确性校验；必要时改用保留完整输出的后处理。  
4. **确保模型路径与代码加载** – 将 `path` 设置为实际模型权重目录，确认 `trust_remote_code=True`（或相应配置）以加载必要的自定义模块；在服务启动后验证模型能输出正确的样例。  
5. **统一评估数据集** – 使用官方完整版 GSM8K（而非 lite）进行评测，确保题目数量、难度和答案格式与基线一致；可在评测前对数据集进行校验，排除因版本差异导致的准确率偏差。