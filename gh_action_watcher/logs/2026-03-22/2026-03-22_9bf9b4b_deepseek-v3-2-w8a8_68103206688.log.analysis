类型: 性能精度不达标

根本原因：
1. 答案后处理函数 `extract_non_reasoning_content` 实现错误，导致模型输出的答案被全部过滤掉，准确率跌至 0%。  
2. 模型加载时 `trust_remote_code=False`，阻止自定义代码执行，tokenizer 与模型不匹配，生成内容异常。  
3. 性能基准（baseline 134 token/s）是基于其他硬件或配置测得的，当前 Ascend 环境下实际吞吐仅为约 56 token/s，基准不匹配。  
4. VLLM 批处理参数（`batch_size=4`、`max_num_batched_tokens=8192`、`max_num_seqs=4`）设置过于保守，导致并行度不足，token 产出率低。  
5. 评测脚本使用的数据集路径或答案解析方式与模型输出格式不一致，导致评测结果错误（准确率为 0）。

最可能的修复方案：
- 审查并修正 `extract_non_reasoning_content` 的实现，确保能够正确定位并提取最终答案；可在测试时打印原始输出进行调试。  
- 将 `trust_remote_code` 设置为 `True`，或确保模型目录下包含所需的自定义代码，使 tokenizer 与模型保持一致。  
- 根据实际 Ascend 硬件重新评估性能基准；如基准不可达，适当降低阈值或提升硬件资源（更多加速卡、更大显存）。  
- 调大 VLLM 的 `batch_size`、`max_num_batched_tokens`、`max_num_seqs` 参数，或关闭不必要的 speculative 解码，以提升并行度和 token 吞吐。  
- 校验数据集文件完整性，确认评测脚本使用的答案格式与模型输出相匹配；必要时在评测前加入模型原始输出的日志以便排查。