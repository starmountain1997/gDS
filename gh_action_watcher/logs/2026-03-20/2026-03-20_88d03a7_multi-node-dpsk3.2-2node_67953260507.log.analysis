**类型: 性能精度不达标**

**根本原因（≤5 条）**
1. **模型量化后精度严重下降**：采用 W8A8（INT8 权重+INT8 激活）量化，在 Ascend NPU 上可能缺少合适的量化校准或算子实现，导致模型输出质量骤降，生成内容无法匹配正确答案。
2. **推理参数或生成配置不适配量化模型**：如 `temperature`、`top_p`、`repetition_penalty` 未做相应调优，可能导致生成过程不稳定、重复或截断，进而导致答案缺失。
3. **后处理器 `extract_non_reasoning_content` 与模型输出格式不匹配**：模型生成的链式思考（CoT）内容可能未按预期使用特定分隔符或结构化标记，导致答案提取脚本失效，最终得到 0% 准确率。
4. **Prompt/聊天模板配置错误**：使用的 `gsm8k_gen_0_shot_cot_chat_prompt` 与模型内部的 chat‑template 不兼容，模型可能误解任务，只输出推理过程而未给出最终答案。
5. **模型权重或环境未正确加载到 NPU**：量化模型文件未在 Ascend NPU 上完成正确的映射或加载，可能回退到 CPU 推理或使用默认随机权重，导致生成结果完全错误。

**最可能的修复方案**
1. **恢复或调优量化**：
   - 暂时使用 FP16 或 W8A16（INT8 权重+FP16 激活）进行基线验证，确认量化是否为根因。
   - 若必须使用 W8A8，需在目标硬件上执行离线量化校准（使用代表性校准数据集）并检查 Ascend NPU 的量化算子是否完整。
2. **调整推理参数**：
   - 将 `temperature` 从 0.7（默认）调低至 0.0‑0.2，以降低随机性。
   - 增大 `max_tokens`，确保完整生成（包括最终答案）不被截断。
   - 检查并适当设置 `repetition_penalty`（如 1.1‑1.2）防止模型陷入重复。
3. **修正 Prompt 与后处理**：
   - 确认 `gsm8k_gen_0_shot_cot_chat_prompt` 与模型 chat‑template 匹配（如使用 `<|im_start|>`、`<|im_end|>` 等标记）。
   - 审查 `extract_non_reasoning_content` 实现，确保它能够定位并提取模型输出的最终答案（可能需要正则或基于特殊标记的二次解析）。
4. **验证模型加载与硬件绑定**：
   - 在启动脚本中加入权重完整性校验（如 SHA256），确保模型文件完整。
   - 确认环境变量（如 `ASCEND_VISIBLE_DEVICES`、`VLLM_ASCEND_RT_VISIBLE_DEVICES`）正确指向 NPU，避免 CPU 回退。
5. **完整端到端回归测试**：
   - 先在单节点上使用 FP16 推理复现 95% 基线，验证模型、数据流和后处理均正常。
   - 再逐步切回 W8A8，确认量化后精度下降幅度在可接受范围（如 ≤ 5%），若下降过大则回退到更宽松的量化策略或重新校准。

通过以上步骤可以定位是量化、生成配置、Prompt/后处理还是硬件加载导致精度为 0，并针对性修复，使模型在 Ascend NPU 上的表现恢复到预期水平。
