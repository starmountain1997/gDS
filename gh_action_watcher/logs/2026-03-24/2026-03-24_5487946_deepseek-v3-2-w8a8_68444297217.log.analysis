类型: 代码问题

根本原因（≤5 条）：
1. **编译时 shape 静态化冲突**：在 `torch.compile` (AOTAutograd + Inductor) 环境下，RoPE 的 `cos`/`sin` 张量在编译期间被静态化为固定 shape，而运行时实际输入的 `num_tokens` 与预分配 shape 不一致。
2. **动态 shape 与分段编译不兼容**：使用的 `piecewise_backend`（torch._inductor）未正确处理 `mla_forward` 算子内部 RoPE 所需的动态 `num_tokens`，导致 `rope_forward_triton_siso` 的 `assert cos.shape[0] == num_tokens` 失败。
3. **预计算 RoPE 表的复用问题**：`cos`/`sin` 在模型初始化时按 `max_position_embeddings` 预生成，但在前向传播中（尤其是 Dynamic Split Fuse 场景）输入的 token 数量会变化，未做 shape 广播或切片的适配。
4. **自定义算子 `vllm.mla_forward` 与 Dynamo 编译冲突**：该算子在 `torch_npu` 后端被追踪为 `eval_with_key`，其内部调用 Triton Kernel 时未通过 torch.compile 的 shape 环境正确传播动态尺寸。
5. **配置或参数不匹配**：可能因 `max_model_len`、`gpu_memory_utilization` 等参数导致 `cos`/`sin` 的实际 shape 与推理请求的 `num_tokens` 产生偏差。

最可能的修复方案：
1. **关闭问题路径的 torch.compile**：在 `vllm/vllm/compilation/decorators.py` 中将 `TorchCompileWithNoGuardsWrapper` 排除 `mla_forward` 相关调用，或在配置中设置 `VLLM_TORCH_COMPILE_LEVEL=0`（针对 Ascend 后端）。
2. **修改 RoPE 实现以支持动态 shape**：在 `rope_forward_triton_siso` 中，将 `assert` 改为运行时检查 + 动态切片/广播（如 `cos = cos[:num_tokens]`），而非强制 shape 相等。
3. **统一 cos/sin 的生成时机**：确保 `cos`/`sin` 在每次前向传播时根据实际 `num_tokens` 生成，或在调用 `mla_forward` 前将预计算的 `cos`/`sin` 切片到匹配的 `num_tokens`。
4. **升级/修补 vllm-ascend**：检查 `vllm_ascend/ops/mla.py` 和 `vllm_ascend/attention/sfa_v1.py` 是否有针对 `piecewise_backend` 的兼容性修复，或回滚到非分段编译模式（设置 `torch._inductor.config.split_cat_fx_passes = False`）。
5. **调整 CI 测试配置**：如使用较小的 `max_model_len` 或限制单次推理的 `num_tokens`，以匹配预计算的 `cos`/`sin` shape，或在测试脚本中显式设置 `CUDA_VISIBLE_DEVICES` 并禁用 Dynamic Split Fuse（设置 `enable_dynamic_fusion=False`）。
