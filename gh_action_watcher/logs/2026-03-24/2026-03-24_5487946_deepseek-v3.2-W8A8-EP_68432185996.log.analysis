## 根本原因分析

### 1. **vLLM 服务器请求处理失败**
- AISBench 报告 “All requests failed, can't calculate performance results”
- 性能测试的 batch_size=512 过大，可能导致服务器过载或超时
- Pod 曾出现重启（RESTARTS: 1），表明服务器运行时曾崩溃

### 2. **多节点通信问题**
- `No valid requests for concurrency plot` 警告表明请求未能正确路由到服务器
- `host_ip='10.0.0.211'` 和 `port=6000` 的配置可能存在网络连通性问题
- 多节点环境下代理（Proxy）和服务器之间的通信可能失败

### 3. **性能测试配置不合理**
- 使用了极高的并发参数（batch_size=512, pipeline=1146）
- 非流式场景下生成并发图表时无有效请求数据
- 测试预期与实际服务器处理能力不匹配

### 4. **错误日志不完整**
- 日志中未显示具体的请求错误详情
- AISBench 建议检查 "ERROR log from every responses"，但未在日志中呈现
- 无法直接定位是连接超时、模型推理失败还是API调用错误

### 5. **测试环境资源不足**
- Pod 重启表明可能存在 OOM（内存不足）或资源竞争
- 长时间运行（20分钟）后失败，可能与资源泄漏有关

---

## 修复方案

1. **降低性能测试参数**
   - 将 `batch_size` 从 512 降至 64-128
   - 减少 `pipeline` 并发数，避免服务器过载

2. **检查网络连通性**
   - 验证 `10.0.0.211:6000` 是否可从测试节点访问
   - 确保 Kubernetes Service 和 Pod IP 映射正确

3. **增加错误日志收集**
   - 在 AISBench 执行前启用详细日志级别
   - 保存每个请求的完整响应（包含错误信息）以便调试

4. **增加服务器稳定性检查**
   - 在运行测试前增加健康检查（health check）步骤
   - 验证所有节点上的 vLLM 服务器已就绪后再执行测试

5. **优化资源分配**
   - 增加 Pod 的内存和 CPU 限制
   - 考虑在测试前清理不必要的资源
